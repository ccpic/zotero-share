# exporting — 导出（基本流程，占位）

- 小库手动：文件 → 导出文献库 → Zotero RDF（含 files 目录即带附件）。
- 程序化：按导入的逆过程逐类 dump（父条目/附件/分类/关系），大库分页拉取。
- 导出后先做只读普查再动手：顶层节点数、附件引用是否齐全、
  分类层级有无孤儿。
- 待补充：增量导出、格式对照（BibTeX/RIS/CSL JSON 取舍）。
