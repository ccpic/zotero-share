# ADR-0004（提案，未接受）e2e 默认路径允许灰色

- 日期：2026-09-16；状态：已取代（提案未按原文接受；口径由 ADR-0006 放宽为全量默认启用，2026-09-17）；来源：05 号票 live exchange——用户在冲突已明示后仍坚持灰色默认开启。
- 上下文：ADR-0001 锁死“默认链路与 e2e 默认路径不进灰色”；06 定稿 HITL-1 推荐位为“不用”；scansci-pdf 边界要求灰色须明确选择。05 灰色追问中用户首轮选“默认开启”，经冲突明示后仍坚持。
- 提议：仅 e2e 默认路径允许灰色 fallback（默认链路其余部分不动）；HITL-1 非空桶推荐位改为“用”，仍保留整桶一次确认（不静默启用）；AFK 回退不变（仍跳过灰色记局限）；报告须如实写每篇实际来源。
- 代价：e2e 固定集可复现性下降（灰色命中只断言如实报告、不断言固定 source）；版权与规则风险由用户承担。
- 接受条件：用户明确接受上述代价并签字式确认后，由 handoff 将 ADR-0001 对应句 supersede，并同步改 03 §7 与 06 §1/§5 落字。在此之前 ADR-0001 本体不动，e2e 报告必须显式标注“灰色默认开启（与 ADR-0001 冲突，ADR-0004 未接受）”。
